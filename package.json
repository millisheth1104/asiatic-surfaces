{
  "name": "360viewportal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "build": "echo Static build ready",
    "vercel-build": "echo Static build ready"
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0"
  }
}
