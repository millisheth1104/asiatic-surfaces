{
  "name": "360viewportal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo Static build ready",
    "vercel-build": "echo Static build ready"
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0",
    "@vercel/kv": "^3.0.0"
  }
}
