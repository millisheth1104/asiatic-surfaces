# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp

# generated imagery lands here; keep the SVGs, ignore nothing by default
# (uncomment if you do not want large photos in the repo)
# assets/textures/*.png
# assets/textures/*.jpg

# Supplied sheet source folders (273 MB of originals). The web-ready
# derivatives in assets/gallery/ are what ships; regenerate with
# scratchpad build_gallery.py if the originals are ever needed again.
/45 Degree/
/DIGITAL-20260905T130641Z-1-001/
/Stone/
/WOODEN1/
